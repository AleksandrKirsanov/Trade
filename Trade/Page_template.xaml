<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Trade.Page_template">
    <ContentPage.Resources>
        
    </ContentPage.Resources>
    <Grid  > <!--Общий грид на всю страницу-->
        <Grid.RowDefinitions>
            <!--Строки общего грида-->
            <RowDefinition Height="40"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"> <!--Грид в верхней строке общего грида-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition />

            </Grid.ColumnDefinitions>
            <Label x:Name="PageName" HorizontalTextAlignment="Center" TextColor="Green" TextDecorations="Strikethrough" FontSize="Title"/>
        </Grid>
        
          


        <ScrollView  Grid.Row="1"> <!--Содержимое средней строки общего грида-->
            
            <StackLayout x:Name="ContPage" Margin="10" >
                
                
            </StackLayout>

            
        </ScrollView>



        <Grid Grid.Row="2"> <!--Грид в нижней строке общего грида-->
            <Grid.ColumnDefinitions>
             
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="forward" Text="Вперёд" Grid.Column="2" Clicked="forward_Clicked" />
            <Button x:Name="Context" Text="Оглавление" Grid.Column="1" Clicked="Context_Clicked"/>
            <Button x:Name="back" Text="Назад" Grid.Column="0" Clicked="back_Clicked"/>
            
        </Grid>


    </Grid>
</ContentPage>