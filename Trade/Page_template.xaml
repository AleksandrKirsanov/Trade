<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Trade.Page_template">
    <ContentPage.Resources>
        <ResourceDictionary>

        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid  > <!--Общий грид на всю страницу--> 
        <Grid.RowDefinitions> <!--Строки общего грида-->
            <RowDefinition Height="40"/> 
            <RowDefinition Height="*" />
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0"> <!--Грид в верхней строке общего грида-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition />

            </Grid.ColumnDefinitions>
            <Label x:Name="Titl" Text="Страница" HorizontalTextAlignment="Center" TextColor="Green" TextDecorations="Strikethrough"/>
        </Grid>
        
        



        <ScrollView  Grid.Row="1"> <!--Содержимое средней строки общего грида-->
            
            <StackLayout x:Name="ContPage" Margin="10" >
                
                
            </StackLayout>

            
        </ScrollView>



        <Grid Grid.Row="2"> <!--Грид в нижней строке общего грида-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="forward" Text="Вперёд" Grid.Column="0" />
            <Button x:Name="IncreaseFont" Text="Шрифт+" Grid.Column="1" Clicked="IncreaseFont_Clicked" />
            <Button x:Name="ReduceFont" Text="Шрифт- " Grid.Column="2" Clicked="ReduceFont_Clicked"/>
            <Button Text="Назад" Grid.Column="3"/>
        </Grid>


    </Grid>
</ContentPage>